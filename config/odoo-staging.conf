[options]
addons_path = /mnt/extra-addons
; Override locally with an untracked file if you need the DB manager.
admin_passwd = False
db_host = db
db_port = 5432
db_user = odoo
db_password = odoo
http_port = 8069
log_handler = :INFO
log_level = info
limit_request = 65536
limit_time_cpu = 600
limit_time_real = 1200
max_cron_threads = 2

; Restrict reachable databases on shared hosts.
list_db = False
db_filter = ^oacis.*$

# Staging specific
workers = 0
