[options]
addons_path = /mnt/extra-addons
; Master DB password. NEVER commit real secrets.
; Provide a local untracked override, or hash it via
; Settings > Database > Master Password (stores pbkdf2).
admin_passwd = False
db_host = db
db_port = 5432
db_user = odoo
db_password = odoo
http_port = 8069
log_handler = :INFO
log_level = info
limit_request = 65536
limit_time_cpu = 600
limit_time_real = 1200
max_cron_threads = 1

# Base specific
workers = 0
