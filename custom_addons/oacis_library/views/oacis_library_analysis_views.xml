<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============================================================
         Library Issue Analysis
         ============================================================ -->
    <record id="oacis_library_issue_pivot_view" model="ir.ui.view">
        <field name="name">oacis.library.issue.pivot</field>
        <field name="model">oacis.library.issue</field>
        <field name="arch" type="xml">
            <pivot string="Library Issue Analysis">
                <field name="issue_state" type="row"/>
                <field name="fine_amount" type="measure"/>
            </pivot>
        </field>
    </record>

    <record id="oacis_library_issue_graph_view" model="ir.ui.view">
        <field name="name">oacis.library.issue.graph</field>
        <field name="model">oacis.library.issue</field>
        <field name="arch" type="xml">
            <graph string="Library Issue Analysis" type="bar">
                <field name="issue_state" type="row"/>
                <field name="fine_amount" type="measure"/>
            </graph>
        </field>
    </record>

    

    <!-- ============================================================
         Library Book Analysis
         ============================================================ -->
    <record id="oacis_library_book_pivot_view" model="ir.ui.view">
        <field name="name">oacis.library.book.pivot</field>
        <field name="model">oacis.library.book</field>
        <field name="arch" type="xml">
            <pivot string="Library Book Analysis">
                <field name="publisher_id" type="row"/>
                <field name="issued_copies" type="measure"/>
            </pivot>
        </field>
    </record>

    <record id="oacis_library_book_graph_view" model="ir.ui.view">
        <field name="name">oacis.library.book.graph</field>
        <field name="model">oacis.library.book</field>
        <field name="arch" type="xml">
            <graph string="Library Book Analysis" type="bar">
                <field name="publisher_id" type="row"/>
                <field name="issued_copies" type="measure"/>
            </graph>
        </field>
    </record>

    

    <!-- ============================================================
         Library Member Analysis
         ============================================================ -->
    <record id="oacis_library_member_pivot_view" model="ir.ui.view">
        <field name="name">oacis.library.member.pivot</field>
        <field name="model">oacis.library.member</field>
        <field name="arch" type="xml">
            <pivot string="Library Member Analysis">
                <field name="member_type" type="row"/>
                <field name="current_issue_count" type="measure"/>
            </pivot>
        </field>
    </record>

    <record id="oacis_library_member_graph_view" model="ir.ui.view">
        <field name="name">oacis.library.member.graph</field>
        <field name="model">oacis.library.member</field>
        <field name="arch" type="xml">
            <graph string="Library Member Analysis" type="bar">
                <field name="member_type" type="row"/>
                <field name="current_issue_count" type="measure"/>
            </graph>
        </field>
    </record>

    

</odoo>
