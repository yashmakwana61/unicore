<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============================================================
         Hostel Allocation Analysis
         ============================================================ -->
    <record id="oacis_hostel_allocation_pivot_view" model="ir.ui.view">
        <field name="name">oacis.hostel.allocation.pivot</field>
        <field name="model">oacis.hostel.allocation</field>
        <field name="arch" type="xml">
            <pivot string="Hostel Allocation Analysis">
                <field name="allocation_state" type="row"/>
                <field name="total_hostel_fee" type="measure"/>
            </pivot>
        </field>
    </record>

    <record id="oacis_hostel_allocation_graph_view" model="ir.ui.view">
        <field name="name">oacis.hostel.allocation.graph</field>
        <field name="model">oacis.hostel.allocation</field>
        <field name="arch" type="xml">
            <graph string="Hostel Allocation Analysis" type="bar">
                <field name="block_id" type="row"/>
                <field name="total_hostel_fee" type="measure"/>
            </graph>
        </field>
    </record>

    

    <!-- ============================================================
         Hostel Maintenance Analysis
         ============================================================ -->
    <record id="oacis_hostel_maintenance_pivot_view" model="ir.ui.view">
        <field name="name">oacis.hostel.maintenance.pivot</field>
        <field name="model">oacis.hostel.maintenance</field>
        <field name="arch" type="xml">
            <pivot string="Hostel Maintenance Analysis">
                <field name="maintenance_state" type="row"/>
                <field name="resolution_days" type="measure"/>
            </pivot>
        </field>
    </record>

    <record id="oacis_hostel_maintenance_graph_view" model="ir.ui.view">
        <field name="name">oacis.hostel.maintenance.graph</field>
        <field name="model">oacis.hostel.maintenance</field>
        <field name="arch" type="xml">
            <graph string="Hostel Maintenance Analysis" type="bar">
                <field name="issue_type" type="row"/>
            </graph>
        </field>
    </record>

    

    <!-- ============================================================
         Hostel Block Analysis
         ============================================================ -->
    <record id="oacis_hostel_block_pivot_view" model="ir.ui.view">
        <field name="name">oacis.hostel.block.pivot</field>
        <field name="model">oacis.hostel.block</field>
        <field name="arch" type="xml">
            <pivot string="Hostel Block Analysis">
                <field name="campus_id" type="row"/>
                <field name="occupied_beds" type="measure"/>
                <field name="available_beds" type="measure"/>
            </pivot>
        </field>
    </record>

    <record id="oacis_hostel_block_graph_view" model="ir.ui.view">
        <field name="name">oacis.hostel.block.graph</field>
        <field name="model">oacis.hostel.block</field>
        <field name="arch" type="xml">
            <graph string="Hostel Block Analysis" type="bar">
                <field name="campus_id" type="row"/>
                <field name="occupied_beds" type="measure"/>
            </graph>
        </field>
    </record>

    

    <!-- ============================================================
         Hostel Room Analysis
         ============================================================ -->
    <record id="oacis_hostel_room_pivot_view" model="ir.ui.view">
        <field name="name">oacis.hostel.room.pivot</field>
        <field name="model">oacis.hostel.room</field>
        <field name="arch" type="xml">
            <pivot string="Hostel Room Analysis">
                <field name="block_id" type="row"/>
                <field name="current_occupancy" type="measure"/>
            </pivot>
        </field>
    </record>

    <record id="oacis_hostel_room_graph_view" model="ir.ui.view">
        <field name="name">oacis.hostel.room.graph</field>
        <field name="model">oacis.hostel.room</field>
        <field name="arch" type="xml">
            <graph string="Hostel Room Analysis" type="bar">
                <field name="block_id" type="row"/>
                <field name="current_occupancy" type="measure"/>
            </graph>
        </field>
    </record>

    

</odoo>
